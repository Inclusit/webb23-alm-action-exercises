name: Pythin Hello World

on: 
  push:
    branches:
      - python-hello-world
    
jobs:
  Python-Hello-World:
    runs-on: windows-latest
    steps: 
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
      uses: actions/setup-python@v2
        with: 
          python-version: '3.x'

        - name: python hello world
          run: python hello.py

